require "test_helper"

describe UsersController do
  # describe "show" do
  #   it "won't let you access a user that doesn't exist" do
  #     bogus_user_id = User.last.id + 1
  #     get user_path(bogus_user_id)
  #     must_redirect_to root_path
  #   end
  #
  #   it "will let you access your own show page" do
  #     user = User.first
  #     login(user)
  #     get user_path(user.id)
  #     must_respond_with :success
  #   end
  # end

end
