require 'test_helper'

describe Recipe do
  describe "initialize" do
    # it "Takes a name" do
    #
    #   recipe_hash = {label: "label", image: "image is here", calories: "calory", diet_labels: ["one", "two"],health_labels: ["one", "two"], ingredients: "ingridients"}
    #   r = Recipe.new(recipe_hash)
    #   r.label.must_equal recipe_hash[:label]
    # end
    #


  end


end
