<section class="recipe-list">

<h2>Results for your search!</h2>

<ul>
  <% @recipes.each do | recipe | %>
    <li>
    <%=link_to recipe.name, ingredients_path + "?#{recipe.uri}" %>
    <%=recipe.image %>

    </li>
    <% end %>
</ul>
</section>
