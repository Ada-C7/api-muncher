<%= @count %> results for your search "<%= params[:q] %>"


<% @recipes.each do |recipe| %>
<ul>
  <li>
    <%= recipe.label %>
    <%= image_tag(recipe.image, :alt => recipe.label) %>
  </li>
</ul>

<% end %>
