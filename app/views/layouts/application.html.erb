<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <header class="main-h">
    <div class="row">
      <div class="small-6 medium 3 column">
        <h1> <%=link_to 'BON APPETIT', recipes_path %></h1>
      </div>

      <div class="small-6 medium 8 column" >
        <%= form_tag(recipes_path, method: "get") do %>
        <div class="input-group input-group-rounded">
          <%= text_field_tag(:q, nil, :placeholder => "Please Enter Your Selection Here..", :class => "input-group-field", :type => "search") %>

        <div class="input-group-button">
          <%= submit_tag "Search", :class => "button-secondary" %>
        </div
            <% end %>
      </div>
    </div>
  </header>

  <body>
    <% flash.each do |name, message| %>
     <h3 class="row status <%= name %>"><%= message %></h3>
     <% end %>
    <%= yield %>
  </body>

    <footer class="footer">
        &copy; 2017 | Powered by
          <%= link_to image_tag("/Edamam_logo_full_RGB.png", :width => 150, :height => 50), "http://developer.edamam.com", target: :_blank %>
    </footer>
</html>
