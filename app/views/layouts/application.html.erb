<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <header>
    <div>
    <%= link_to image_tag('pandalogo.png', class: "logo"), homepages_index_path  %>
  </div>
  <div class="small-12 columns medium-9 columns end large-9 columns end">
    <h1><%= link_to "Panda-licious", root_path %></h1>
    <h5> What are you hungry for?</h5>
  </div>
  </header>

  <body>
  <% if flash[:result_text] or flash[:messages] %>
    <section class="row status <%= flash[:status] %>">
      <h3><%= flash[:status] == :failure ? "A problem occurred: " : "" %><%= flash[:result_text] %></h3>
      <% if flash[:messages] %>
        <ul>
          <% flash[:messages].each do |name, problems| %>
            <% problems.each do |problem| %>
              <li><%= name %>: <%= problem %></li>
            <% end %>
          <% end %>
        </ul>
      <% end %>
    </section>
  <% end %>


    <%= yield %>

  </body>
  <footer>
    <%= link_to (image_tag "edamamlogo.png"), "http://developer.edamam.com" %>
  </footer>
</html>
