<% @list_of_recipes.each_with_index do |recipe, index| %>
  <div class="recipe_result large-4 columns">
    <img src="<%= recipe.image_url %>"  alt="picture of recipe: <%= index + 1 %>">
    <div><h6><%= link_to recipe.label, recipe_path(recipe.id) %></h6></div>
  </div>
<% end %>
