<h1>Search Results</h1>
  <% @list_of_recipes.each_with_index do |recipe, index| %>
    <div class="large-4 columns">
      <img src="<%= recipe.image_url %>"  alt="recipe: <%= index + 1 %>">
      <div><h5><%= link_to recipe.label, recipe_path(recipe.id) %></h5></div>
    </div>
  <% end %>
