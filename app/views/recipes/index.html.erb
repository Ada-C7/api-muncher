<h1>Search Results</h1>

<section class="recipes-list">
<ul>
  <% @recipes.each do |recipe| %>
  <li>
    <%= link_to image_tag(recipe.image, class: "preview"), recipe.url, target: "_blank" %>
    <%= link_to recipe.label, recipe.url, target: "_blank" %>
  </li>
  <% end %>
</ul>
</section>
