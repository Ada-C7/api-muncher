<h1>Recipes#index</h1>
<p>Hello</p>

<div class="row" >
  <div class=column >
    <%= form_tag(recipes_path, method: :get) do %>
      <%= label_tag(:q, "Search for:") %>
      <%= text_field_tag(:q) %>
      <%= submit_tag("Search") %>
    <% end %>
  </div>
</div>


<!-- <section class="row"> -->
<div class="row small-up-1 medium-up-2 large-up-3">
<% @recipes.each do |recipe| %>
    <!-- <%#= link_to recipe.label, recipe_path(:label => recipe.label, :uri =>recipe.uri) %> -->
    <div class="column column-block">
    <%= link_to image_tag(recipe.image), recipe_path(:label => recipe.label, :uri =>recipe.uri), :title => recipe.label %>
  </br><span class="title" ><%= link_to recipe.label, recipe_path(:label => recipe.label, :uri =>recipe.uri) %></span>
  </div>
<% end  %>

<%= will_paginate @recipes_pages, class: "row"%>
</ul>

</div>
<!-- </section> -->
