<h1>Recipes#index</h1>

<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <%= link_to recipe.label, recipe_path(recipe.label) %>
    </li>
    <li>
      <%= image_tag(recipe.image) %>
    </li>
  <% end %>
</ul>

<%= will_paginate @recipes %>
