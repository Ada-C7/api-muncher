<h1>All the recipes</h1>

<% @recipes.each do |recipe| %>
  <%= link_to image_tag(recipe.image, alt: "Recipe img"), recipe_path(recipe.id, "q" => @search_term) %>
  <h4><%= recipe.name %></h4>
<% end %>
