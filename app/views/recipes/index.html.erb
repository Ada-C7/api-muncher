<%= form_tag(recipes_path, method: :get) do %>
     <%= label_tag(:filter) %>
     <%= text_field_tag :filter %>
     <%= submit_tag 'Filter' %>
<% end %>

<% @recipes.each do | recipe | %>
     <p> <%= link_to recipe.name, recipe_path(recipe.id) %><p>
     <p> <%= link_to image_tag(recipe.photo), recipe_path(recipe.id) %><p>
<% end %>

<%= paginate @recipes %>
