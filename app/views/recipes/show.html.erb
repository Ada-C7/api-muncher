<section>
  <h2 class="small-12 columns"><%= @recipe.label %></h2>
  <section class="medium-3 small-12 columns">
    <%= image_tag @recipe.image, alt: @recipe.label %>
  </section>
  <section class="medium-9 small-12 columns recipe-details">
    <p>
      <% (@recipe.diet_labels + @recipe.health_labels).each do |food_info| %>
        &check; <%= food_info %>
      <% end %>
    </p>
    <p>
      <strong>Calories:</strong> <%= @recipe.calories.round(2) %>
    </p>
    <h4>Ingredients:</h4>
    <ul>
      <% @recipe.ingredients.each do |ingredient| %>
        <li><%= ingredient %></li>
      <% end %>
    </ul>
    <%= link_to "See full recipe", @recipe.recipe_link, target: "_blank" %>
  </section>
</section>
